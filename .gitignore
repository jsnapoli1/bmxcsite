node_modules
dist
.worktrees
.superpowers
.wrangler/

# Local-only admin layout preview (see the comment inside it). Stubs the
# Access-gated reads so the panel's layout can be viewed on localhost.
admin-preview.html

# Local browser-verification screenshots.
.screenshots/

# Local secrets for `wrangler dev`. Never commit: this holds real
# credentials (CF_API_TOKEN, SHOP_ADMIN_PASSWORD).
.dev.vars
.dev.vars.*
